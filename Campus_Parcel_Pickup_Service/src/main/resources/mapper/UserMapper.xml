<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.admin.mapper.UserMapper">

    <!-- 用户列表查询结果映射 -->
    <resultMap id="UserListDTOMap" type="com.team.admin.dto.UserListDTO">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="name" property="name"/>
        <result column="phone" property="phone"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="dormitory_area" property="dormitoryArea"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 分页查询用户列表（支持学号/姓名模糊搜索） -->
    <select id="selectUserListWithSearch" resultMap="UserListDTOMap">
        SELECT 
            id, student_id, name, phone, role, status, dormitory_area, create_time, update_time
        FROM user
        <where>
            <if test="keyword != null and keyword != ''">
                AND (student_id LIKE CONCAT('%', #{keyword}, '%') 
                     OR name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
